local lush = require "lush"
-- justinmk_vim_dirvish:{{{
local justinmk_vim_dirvish = lush(function()
  return {
      -- DirvishPathTail {},
      -- DirvishArg {}
  }
end)
return justinmk_vim_dirvish
-- }}}
