local lush = require "lush"
-- Diff: {{{
local Diff = lush(function()
  return {
    -- diffAdded {},
    -- diffRemoved {},
    -- diffChanged {},
    -- diffOldFile {},
    -- diffNewFile {},
    -- diffFile {},
    -- diffLine {},
    -- diffIndexLine {},
  }
end)
return Diff
-- }}}
