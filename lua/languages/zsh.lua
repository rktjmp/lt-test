local lush = require "lush"
-- Zsh: {{{
local Zsh = lush(function()
  return {
    -- builtin: https://github.com/chrisbra/vim-zsh{{{
    --   zshOption {},
    --   zshSubst {},
    --   zshFunction {},
    -- }}}
  }
end)
return Zsh
-- }}}
