local lush = require "lush"
-- Toml: {{{
local Toml = lush(function()
  return {
    -- tomlTable {},
    -- tomlKey {},
    -- tomlBoolean {},
    -- tomlString {},
    -- tomlTableArray {}
  }
end)
return Toml
-- }}}
