local lush = require "lush"
-- Makefile: {{{
local Makefile = lush(function()
  return {
    -- builtin: {{{
    -- makeIdent {},
    -- makeSpecTarget {},
    -- makeTarget {},
    -- makeCommands {},
    -- }}}
  }
end)
return Makefile
-- }}}
