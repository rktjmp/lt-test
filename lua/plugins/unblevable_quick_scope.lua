local lush = require "lush"
-- unblevable_quick-scope:{{{
local unblevable_quick_scope = lush(function()
  return {
    -- QuickScopePrimary {},
    -- QuickScopeSecondary {},
  }
end)
return unblevable_quick_scope
-- }}}
