local lush = require "lush"
-- justinmk_vim_sneak:{{{
local justinmk_vim_sneak = lush(function()
  return {
    -- Sneak {},
    -- SneakLabel {},
    -- SneakScope {},
  }
end)
return justinmk_vim_sneak
-- }}}
